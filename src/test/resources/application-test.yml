spring:
  datasource:
    url: jdbc:h2:mem:bojchallenge;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  jackson:
    time-zone: Asia/Seoul

jwt:
  secret: test-secret-should-be-long-32-chars!!!
  expiration-ms: 3600000
